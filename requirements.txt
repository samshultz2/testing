# Direct dependencies (human-maintained). The fully-resolved, hash-pinned
# transitive tree lives in requirements.lock — CI and Linux production install
# from that with `pip install --require-hashes -r requirements.lock` for
# reproducible, integrity-verified builds. Regenerate the lock after editing
# this file:  uv pip compile requirements.txt --generate-hashes \
#             --python-version 3.11 -o requirements.lock
Flask==3.1.3            # 3.0.3 -> CVE-2026-27205
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Werkzeug==3.1.6         # CVE-2025-66221 / 2026-21860 / 2026-27199 (also req. by Flask 3.1)
# Pulled in transitively by Flask/Werkzeug, but imported directly by app code
# (utils/hr.py signs tokens with itsdangerous; utils/barcode_svg.py escapes with
# markupsafe), so declared explicitly — versions match the resolved lock.
itsdangerous==2.2.0
MarkupSafe==3.0.3
SQLAlchemy==2.0.23
# PostgreSQL driver (psycopg 3). Needed when DATABASE_URL points at Postgres
# (postgresql+psycopg://...). Not required for the default SQLite backend.
psycopg[binary]==3.2.3
openpyxl==3.1.2
python-pptx==1.0.2
# pandas 2.2+ supports numpy 2.x, which ortools (below) requires. Older 2.1.x
# pinned numpy<2 and conflicted with ortools' numpy>=2.0.2.
pandas==2.2.3
python-dateutil==2.8.2
Pillow==12.2.0         # 10.4.0 -> PYSEC-2026-165, CVE-2026-25990/40192/42310/42311
pytesseract==0.3.13
python-docx==1.1.2
reportlab==4.2.5
PyMuPDF==1.24.10
python-dotenv==1.2.2   # 1.0.1 -> CVE-2026-28684
# OR-Tools constraint solver: powers the "Generate with OR-Tools" (Optimal)
# timetable option. Without it, that option flashes an error and does not
# generate, so the timetable falls back to the greedy result (more empty slots).
ortools==9.15.6755
# pytesseract needs the Tesseract OCR engine installed on the system
# (e.g. `apt-get install tesseract-ocr` or `brew install tesseract`).
# PyMuPDF (fitz) reads PDFs and renders scanned pages for OCR (pure pip, no
# system deps).

# Production WSGI server (used by wsgi.py / scripts/start.sh in production).
gunicorn==23.0.0

# Pure-Python WSGI server used by app_production.py (the phone/Cloudflare
# launcher). Cross-platform and easy to install on Termux.
waitress==3.0.2

# QR code for the in-app "Share App" panel (pure Python, SVG output — no Pillow).
qrcode==8.2

# HTTP client for outbound calls (Paystack billing). Declared explicitly with a
# compatible urllib3 so a stray transitive combo can't trigger
# "RequestsDependencyWarning: urllib3 ... doesn't match a supported version" or,
# worse, hang a request. requests 2.32+ supports urllib3 2.x.
requests==2.32.3
urllib3==2.2.3

# HTML parsing for the MySchool past-question harvester (utils/myschool.py).
# Uses the built-in html.parser backend, so no lxml is required.
beautifulsoup4==4.15.0

# Database migrations (Alembic via Flask-Migrate). `flask db ...` commands;
# schema is tracked in db_migrations/. See docs/MIGRATIONS.md.
Flask-Migrate==4.1.0

# Response compression (gzip) — much smaller pages on slow/poor connections.
Flask-Compress==1.24

# AES-256-GCM field encryption at rest (utils/crypto.py). Only used when
# FIELD_ENCRYPTION_KEY is configured.
cryptography==49.0.0

# Dev/test
pytest==9.0.3          # 8.3.3 -> CVE-2025-71176

# Optional: enables the Claude-vision OCR fallback (set OCR_VISION_FALLBACK=1 and
# ANTHROPIC_API_KEY). Not required for the default Tesseract OCR.
# anthropic==0.69.0

# Optional: external error monitoring via Sentry. Activate by installing this and
# setting SENTRY_DSN (see utils/monitoring.py). The app runs fine without it —
# the always-on in-app Error Log + email alerts need no extra packages.
# sentry-sdk[flask]>=2.0

# Optional: browser Web Push (utils/webpush.py). Install this and set the VAPID
# keys (WEBPUSH_VAPID_PUBLIC_KEY / _PRIVATE_KEY / _CLAIMS_EMAIL) to enable push;
# without it, push sends are a safe no-op and subscriptions are simply stored.
# pywebpush>=2.0.0
